services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - "$PWD:$PWD"
    working_dir: "$PWD"
    network_mode: "host"
    env_file:
      - .env
    command: python3 main.py